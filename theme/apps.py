"""The ThemeConfig class is the Django app configuration for the 'theme' app.

It defines the name of the app, which is 'theme'.
"""

from django.apps import AppConfig


class ThemeConfig(AppConfig):
    """The ThemeConfig class is the Django app configuration for the 'theme' app.

    It defines the name of the app, which is 'theme'.
    """

    name = "theme"
